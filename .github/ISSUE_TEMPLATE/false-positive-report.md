---
name: False-positive report (official endpoint FAILed)
about: An official, direct-connection endpoint FAILed an audit against its own freshly
  enrolled reference. This challenges our FPR claim and we want to know about it.
labels: fpr-challenge
---

**Endpoint** (official API base URL + model name):

**How the reference was enrolled** (exact command, `--samples`, date):

**Audit command used**:

**Attach both JSON files** (the `ref*.json` and the audit report):

**Anything unusual** (proxy in between, region, time gap between enroll and audit,
provider status page incidents):
